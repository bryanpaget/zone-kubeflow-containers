{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "dependencyDashboard": true,
  "ignoreDeps": [
    "k8scc01covidacr.azurecr.io/sas4c"
  ],
  "customManagers": [
    {
      "customType": "regex",
      "description": "Update VSCode extensions in Dockerfile",
      "fileMatch": ["^images/mid/Dockerfile$"],
      "matchStrings": [
        "code-server\\s+--install-extension\\s+(?<depName>[^@\\s]+)@(?<currentValue>[^\\s]+)"
      ],
      "datasourceTemplate": "my-open-vsx-registry",
      "packageNameTemplate": "{{depName}}",
      "versioningTemplate": "semver",
      "matchStringsStrategy": "any"
    }
  ],
  "customDatasources": {
    "my-open-vsx-registry": {
      "datasource": "open-vsx",
      "defaultRegistryUrl": "https://open-vsx.org/api"
    }
  },
  "packageRules": [
    {
      "matchDatasources": ["my-open-vsx-registry"],
      "enabled": true,
      "rangeStrategy": "replace"
    }
  ]
}
